# 项目概况总览

> **了解项目先读这一份。** 最后更新：2026-09-19。这是一份活的看板：进度有大变化时先更新这里，再动别处。
> 当前详情与边界见 [[docs/remaining-work.md]]；生产部署见 [[docs/deploy.md]]；文档索引见 [[docs/README.md]]。

## 零、2026-09-19 最新快照

### 代码与产品

- GitHub `origin/main` 已到 `dbf1281`：完成双端 UI 重设计、正式域名部署，并修通生产环境真实账号注册、登录、持久化会话、退出和账号删除链路。
- 前端已可生产静态导出；本次在新鲜工作树中验证 **21 个路由构建通过**。
- 后端已扩展身份与角色、课程建设、教材编目/预算/摄取、学生记忆、Sandbox 生命周期、治理、验证工具、视觉识别与学习平台 API。
- 陈纪修《数学分析》第三版已有 16 章候选知识文件；教材全量识别、页级复核、预算账本和候选索引已形成离线产物，但未审校内容仍与正式检索隔离，不冒称整本教材已通过数学正确性验收。

### 当前验收结果

| 检查 | 2026-09-19 结果 | 解读 |
|---|---|---|
| 前端生产构建 | ✅ 通过 | 21 个静态路由导出成功 |
| 后端 `pytest` | ✅ 99 通过 | 新增正式域名注册、6 位口令、持久会话、重复账号与恶意 Origin 拒绝回归；原有 CoursePack/RAG 测试继续通过 |
| Caddy 生产配置 | ✅ 官方容器校验通过 | DNS 生效后自动申请 HTTPS 证书 |
| 真机/教学质量 | ⚠️ 未完成 | 不能用页面可点击或 Stub 测试替代真实课堂、真实模型与手机拍照验收 |

### 上线状态

- 生产服务器：`124.220.5.87`；已于 2026-09-19 升级到 `dbf1281`，Caddy 首页、`/student`、`/api/health` 与账号 API 已在正式域名验收通过。升级前已完成 PostgreSQL 压缩备份与 `.env` 备份。
- `learningmirror.cn` 与 `www.learningmirror.cn` 已解析到 `124.220.5.87`；Caddy 已为两个域名签发 Let's Encrypt 生产证书。
- ICP 备案号 `浙ICP备2026075928号-1` 已写入生产构建并在页底链接工信部。
- 腾讯云轻量应用服务器已放行公网 TCP 443；两个域名 HTTPS 均返回 200，证书校验正常。
- 学生可直接创建真实账号，口令至少 6 位且注册时须二次确认；教师/TA 仍须私有邀请码。生产烟测已完成“注册 → Cookie 会话恢复 → 退出 → 重新登录 → 删除测试账号”全链路。

### 截止 09-30 的最高优先级

1. 启动小范围真实账号试用，观察注册失败率、会话稳定性与异常登录频率，并完善忘记口令/管理员重置流程。
2. 执行 PostgreSQL 数据库备份、升级和回退演练，并保持 99 项回归继续通过。
3. 完成正式作业原图附件存储/权限/到期删除，并做手机真机与真实视觉识别验收。
4. 扩大数分教师审校、真题评测、拒答/泄题评测，再冻结提交版、演示脚本和 `submission` tag。

> [!warning] 历史快照
> 下方保留 2026-08-28 的目标与背景，用于理解项目演进；与上方冲突时，以 2026-09-19 快照和 [[docs/remaining-work.md]] 为准。

---

### ZJU 校园桌面集成预研

- 已完成对 CampusOS 固定提交的代码级核查，详细报告见 [[docs/research/CampusOS-ZJU集成可行性评估.md]]。
- 可复用产品思路：把课表、考试、DDL、个人日程映射为统一事件，并保留来源、更新时间和刷新状态。
- 不可直接复用其认证架构：CampusOS 是 Electron 本地代登录，密码受本机 `safeStorage` 保护；将相同逻辑搬到公网云端会令平台集中接触浙大密码、Cookie 与敏感学业数据。
- 正式路径：由校内单位申请统一身份认证应用接入，并分别取得教务、学在浙大、素拓、智云等数据主管部门的只读接口授权；统一认证本身不等于取得这些业务数据权限。
- 在批准前可先做个人日程、ICS/CSV 导入、公开校历/校园资讯和官方系统深链；不得在腾讯云收集浙大密码或批量保存业务会话。
- 当前阻塞：确认校内主办单位/指导教师、申请数据权限，并确认涉及师生个人或敏感数据的系统部署位置与校外备案要求。
- 2026-09-19 已按开发者预览范围新增 `apps/zju-connector`：仅在学生本机处理账号、密码与 Cookie，首批支持本科课表、学在浙大 DDL 和最小化 JSON 导出；不部署到腾讯云。

## 一、我们在做什么（一段话）

做一个**面向大学数理课程的个性化学习智能体平台**（作品名称：**学镜 / Learning Mirror**；早期文档中的 MathMirror 为同一产品），三个角色分工：

- **学生端（Student Mirror）**：首页并列为课程教学与 AI 教学。数理课卡住时给"最小有效提示"；Vibe coding / Agent 使用同一套规矩。不打分、不贴标签；匿名参与码保护隐私。
- **课程端（Course Mirror）**：每门课一个专业智能体，装着这门课的知识、解法结构、提示阶梯和质检题；越教越懂课，跨届共享；
- **教师端（Assignment Workspace + 教师/TA 界面）**：把"这个班这周做作业时发生了什么"变成教师可行动的报告；AI 提候选结论，TA 负责确认/驳回，教师有最终决定权，且**默认看不到学生私密对话**。

**技术路线（关键，别搞混）**：我们**不训练大模型**。用国内通用大模型当大脑 + 自建课程语料当资料（RAG 检索，查资料不是训练）+ 智能体代码当工作流程 + 评测题当质检。教材数据授权范围本来就只开了"检索"，训练权限关闭，正好与这条路一致。

**课程范围（严格限定，不扩）**：数学分析（深做旗舰）+ 高等代数与解析几何、大学物理、点集拓扑、常微分方程（真实可用），共 **5 门**。

---

## 二、总体要达到什么效果

### 最终场景（验收标准就是这个画面）

> 周三晚上，学生小王做数分作业，在一道 ε-δ 证明上卡住。他拍题、自己先想，然后点"我卡住了"。系统结合他的历史（曾在构造辅助对象时卡住过两次）只给第一级提示，而不是答案。他做出来后，系统记下一条学习证据——**全程不用填任何问卷**。
> 同一时间，TA 收到 AI 的批改辅助："这份作业里有 4 名学生出现相同的定理条件误解"。TA 确认后，教师的周报里清楚地写着：该现象覆盖 4/32 人，"作业证据"和"过程证据"分开标注、不外推全班。教师据此决定下周一花十分钟讲这个问题。
> 学生的私人对话内容，教师全程看不到。

### 比赛目标（硬截止）

| 时间 | 事项 | 状态 |
|---|---|---|
| **09-15 前（剩 18 天）** | 官网注册报名 + 申报书 | ❌ 未开始 |
| **09-30 前（剩 33 天）** | 提交作品及说明文件，打 `submission` tag | ❌ 未开始 |
| 下半年 | 现场决赛：现场展示 + 答辩 | — |

现场展示要求：**三端（学生/课程建设/教师·TA）+ 五门课程全部可用**，体现"师-生-机"协同。评审看：创新性、教学应用价值、技术实施质量、用户体验与可推广性、落地效果。

### 产品目标（比赛之外真正想做的）

平台要能**真实邀请学生内测、帮助教学**。邀请学生前必须先完成：校方审批、责任教师确认、隐私说明、权限演练——流程周期不可控，**宜尽早启动**。

---

## 三、目前做到哪了（按工作流，团队名单未定）

| 工作流 | 内容 | 成果在哪 | 状态 |
|---|---|---|---|
| 产品设计 | v0.2 参照文档（三对象、假设—证据—更新、双通道证据、1+4 策略） | 本仓库 `docs/MathMirror_产品角色与开发参照_v0.2.md` | ✅ 定稿 |
| 阶段 0 骨架 | 五门课注册表、Course Mirror 协议、三端页面骨架、基础设施配置 | 本仓库 | ✅（本机全栈环境已验证可运行，含 Docker 三服务） |
| 阶段 1–3 平台底座 | 通用 Course Mirror 协议、20 表数据库、502MB 教材授权入库（102 个来源，授权门控）、PaddleOCR、数分第一章试点（6 个审核节点 + 1 例题）、19 项测试 | ⚠️ 另一台电脑（`C:\Users\19900\learning-mirror-platform`） | 旧版完成但**未入仓库**；阶段 1 基座已于 08-27 在本仓库重建（数据库 + 统一管线 + CoursePack 管道，15 项测试） |
| v5 比赛演示闭环 | 可交互学生/教师端（合成数据）、个性化规则（同题不同提示）、25 项测试 | ⚠️ 同上 | 完成，**未入仓库** |
| v6 真实内测系统 | 匿名参与码、授权分离、幂等事件、草稿默认不存、TA/教师审计链、30 项测试 + 真实 HTTP 链路验收 | ⚠️ 同上 | 完成，**未入仓库** |
| 大赛事务 | 报名、赛道确认（建议"智能体"）、团队、申报书 | — | ❌ 未开始 |

**一句话总结：三端已拿下两端（学生端、教师/TA 端均真实可用），课程建设端还是骨架；两个最大问题——大部分旧代码在仓库外，大赛事务还没启动。**

### 成果在哪里（08-28 快照）

| 成果 | 位置 | 状态 |
|---|---|---|
| 平台代码：阶段 0 骨架 + 已重建的阶段 1 基座（数据库、统一请求管线、CoursePack 管道、19 项测试） | 本仓库 `main`（GitHub） | ✅ 已推送 |
| 学生端交互页面：选题 → 渐进提示 → 完整解答 → 知识点答疑（含质检与证据展示、公式渲染、匿名参与码与作业加入） | 本仓库 `apps/web`，`pnpm dev:web` 后访问 `/student` | ✅ 已推送 |
| 教师/TA 端作业工作区闭环：工作区管理（加入码）→ 聚合总览 → AI 候选现象 → TA 三选一校准 → 教师最终决定 → 周报（隐私红线在数据层强制） | 本仓库 `apps/web` `/teacher` + 10 个工作区端点 | ✅ 已推送（真实模型验证通过） |
| 模型网关接入 DeepSeek 真实模型（`deepseek-v4-flash`，密钥仅存本机 `.env` 不入库） | 本机 `.env` + 仓库网关代码 | ✅ 已验证（10 题提示泄露红线全过） |
| 数分旗舰语料：第一章自编知识节点与题目 | 本仓库 `coursepacks/mathematical_analysis/` | ✅ 已入库（11 节点 + 10 题），待人工核验 |
| 阶段 1–3 旧版、v5/v6 代码、502MB 教材入库成果 | 另一台电脑，未入仓库 | ⚠️ 待 A 组找回 |
| 大赛提交材料包（`submission/` 目录 + `submission` tag） | 本仓库，待建 | ❌ 09-30 前 |

> 线上查看入口：GitHub 仓库 <https://github.com/pengjinrui278/qizhen_ai-driven_teaching_platform>。本机运行方式见根目录 `README.md`。

---

## 四、下一步怎么走

| 优先级 | 事项 | 谁主导 | 时间 |
|---|---|---|---|
| 🔴 1 | **代码找回**：拷贝/推送 `learning-mirror-platform` → 导入本仓库 → 全量验证（A 组） | 队员操作，AI 导入 | 本周 |
| 🔴 2 | **报名 + 确认团队名单**（截止 09-15，B 组） | 队员 | 本周 |
| 🟠 3 | **数分智能体深链路**：大模型已接入（DeepSeek）→ 第一章语料人工校对 → 向量混合检索 → Eval 测试（C 组） | AI 为主 | 1–2 周 |
| 🟠 4 | **内测合规申请**：校方审批、隐私说明（D5，周期长，尽早启动） | 队员 | 尽早 |
| 🟡 5 | **三端最后一端**：课程建设端页面从骨架走向可用（CoursePack 导入/查看/质检状态） | AI 为主 | 尽快 |
| 🟡 6 | **内测功能打磨**：模型评阅器、拍题/草稿分析、数据导出/删除（D 组；教师决策界面已完成） | AI 为主 | 3 之后 |
| 🟡 7 | **四门课复制**到最小可用（E 组，严格只限这 4 门） | AI 为主 | 提交前 |
| 🟢 8 | **提交材料包 + 演示脚本**（三端 + 五门，B5） | 共同 | 09-30 前一周 |

---

## 五、主要风险

1. **代码不在仓库**：丢失风险 + 无法协作，第一优先级解决；
2. **报名只剩 18 天**，且依赖团队名单（尚未确认）；
3. **内测合规周期不可控**：想邀请真实学生，审批必须现在启动；
4. **约 502MB 原始教材**不能进仓库，存放方案待敲定（建议仓库外保存 + 登记校验和与授权范围）。

---

## 六、想深入了解看哪里

| 想知道 | 读这个 |
|---|---|
| 产品完整设计（为什么这么做、每端长什么样） | `docs/MathMirror_产品角色与开发参照_v0.2.md` |
| 开发阶段划分（阶段 0–6） | `docs/development-roadmap.md` |
| 技术架构与模块边界 | `docs/architecture.md` |
| 大赛要求与行动清单 | `docs/competition/参赛指南.md` |
| 数据版权红线 | `docs/data-rights.md` |
| 详细任务清单（A–E 五组） | `docs/progress/进度整理-2026-08-27-现状与下阶段任务.md` |
| 历次重大更新记录 | `docs/progress/进度日志.md` |
| 选题与实施的原始讨论记录（一般不用读） | `docs/archive/` |
