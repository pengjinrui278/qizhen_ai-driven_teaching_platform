# 智能体教学优化（进行中）

## 已完成本地实现

- 所有学生课程模式统一引导式约束，旧 full_solution 接口保留兼容但改为带空缺的解题框架；离线模式不再输出完整解法。
- 七级提示具有不同支架边界；重复首提示不回退，七级后返回耗尽状态。静态答案的精确泄漏检查扩展到各课程模式，但不能声称能检测所有语义上的代答。
- 教材块的定位字段传入模型；教材与历史设置 UTF-8 字节预算，优先保留最新提问，截断明确标记。引用只保留进入上下文的材料，不等于每一条已被回答逐句引用。
- 课程 profile 可用 metadata.teaching_guidance 定制教学要求；未知课程保留通用安全边界。
- 检索加入概念别名、词项区分度、标题权重及重复内容去重，排除孤立数字和变量。仍属于词项检索，不冒充语义向量检索。

## 验收

- 2026-09-25 合并队友更新后完整后端离线回归：154 项通过，1 条 Starlette/httpx 弃用告警。前端构建成功（26 页）；4 项真实 Markdown/KaTeX 渲染测试通过。
- 同日合成 API 浏览器回归通过：SSE 错误与同 ID 重试、数学公式、新对话清理、历史恢复、手机宽度。首次因本地前端未启动失败，启动后复测通过；不代表真实模型教学验收。
- 正式站已用专用验收账号实际提问，返回 deepseek-flash，策略仍为 course-student-v2。首级提示直接给出关键构造，说明仍需部署新策略并做真实教学评测。
- 本地更改尚未部署；不将旧线上回复作为新策略验收结果。账号凭据不写入本文件或仓库。

## 仍需完成

- 已实现阶段 SSE 与校验后完整回答（非 token 流）、公式与历史切换、错误重试和拍题概念检索；需要真实模型联合验收。
- 已实现提示使用与自报结果弱证据；更正传播、一致性与诊断质量继续由学习反馈分工复核。
- 教师预分析与人工确认流程复核。
- 有效服务器登录方式或由团队执行部署；部署后使用新策略重新验收。
- 用户已确认 WorkBuddy 安装成功；其他三种工具仍缺干净 Windows 首次安装验证。
