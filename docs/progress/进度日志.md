# 进度日志

> 记录每次**重大更新**：里程碑完成、智能体/模型能力变化、语料库批次扩充、申报材料定稿等。
> 新条目追加在最上方。格式：日期 + 更新内容 + 下一步。

---

## 2026-08-31 学生端新增「上传错题」功能

**更新内容**

- `apps/api/src/mirror_api/domain.py` 新增 `StudentUploadRequest`、`StudentUploadResponse`、`StudentProblemReviewRequest`、`ProblemSummary` 模型；
- 新增 `apps/api/src/mirror_api/upload_service.py`：识别题库已有题、未命中则创建学生上传题、生成 5 级不泄露答案的提示阶梯、自动质量评估、推荐 3 道同类题；
- `apps/api/src/mirror_api/retrieval.py` 新增 `find_similar_problems`，按共享知识节点与文本 token 重合度计分；
- `apps/api/src/mirror_api/llm.py` 新增 `hint_ladder_generation` 模式，`StubMirrorModel` 提供确定性占位提示；
- `apps/api/src/mirror_api/mirror_service.py` 允许学生上传题进入渐进提示管线，完整解答在未审批前被门控，新增 `dynamic_hint_safety` harness；
- `apps/api/src/mirror_api/main.py` 新增 `/api/v1/student-uploads`、`/api/v1/student-uploads/pending`、审校端点；
- `apps/web/src/app/student/page.tsx` 新增上传错题面板：多行输入、质量状态 chip、首级提示、3 道相似题卡片，可点击切换练习；
- `apps/web/src/app/styles.css` 补充 `.uploadPanel`、`.uploadTextarea`、`.uploadStatus`、`.similarPanel`、`.similarList` 等主题样式；
- 新增 `apps/api/tests/test_upload_service.py`：7 项测试覆盖识别、创建、提示安全、相似题排除、审校入库、未审批禁用完整解答；
- 后端 44 项测试全绿，前端 `pnpm build` 通过，ruff 检查通过。

**下一步**

- 在真实浏览器端到端验证：上传文本 → 首级提示 → 相似题 → next_hint → full_solution 门控；
- 教师/TA 端接入「学生上传题待审」列表；
- 根据真实模型输出调整提示阶梯生成 prompt 与质量评估阈值。

---

## 2026-08-30 首页卡片改为可点击弹窗介绍

**更新内容**

- `apps/web/src/app/page.tsx` 改为客户端组件，新增 `Modal` 弹窗组件；
- 首页三端角色卡（Student Mirror / Course Mirror / Assignment Workspace）与五门课程卡全部改为可点击，hover 时显示“点击查看详细介绍”；
- 点击后弹出毛玻璃弹窗，展示对应角色或课程的完整介绍，部分弹窗附带直达入口（学生端/教师端）；
- 补充 styles.css：`.interactiveCard` 悬停动效、`.modalBackdrop` / `.modalCard` / `.modalClose` 等弹窗样式；
- 前端构建通过，`pnpm build:web` 成功。

**下一步**

- 在真实浏览器点击验证每个弹窗内容与跳转；
- 继续扩展课程内容或推进部署。

---

## 2026-08-30 全站 UI 改为「海天樱韵」渐变配色并优化布局

**更新内容**

- 主题色从深绿暗色改为 **晴空蓝 #98CBEA → 薄纱粉 #F1DCE4** 的线性渐变背景，整体低饱和莫兰迪柔光风格；
- 正文文字改为深灰 #333/#444，标题使用同源深蓝 #589fd0；
- 所有卡片（首页功能卡、课程卡、题目面板、工作区卡片、消息气泡、统计块等）统一使用 **毛玻璃效果**（rgba 白色底 + backdrop-blur + 柔光阴影）；
- 按钮 redesigned：主按钮用 #589fd0 实心+投影，次按钮用半透明白底+蓝边框；输入框、下拉框改用浅蓝边框+聚焦光晕；
- 学生端/教师端布局间距、圆角、悬停动效统一优化；
- `next.config.js` 同步调整：开发时不启用 `output: export`，避免 dev server 读取 `dist/` 导致 500；
- 前端构建通过，后端 37 项测试全绿。

**下一步**

- 在真实浏览器中截图确认五门课程切换、题目列表、对话区、教师端工作区视觉效果；
- 根据团队反馈微调卡片透明度、阴影强度、移动端适配；
- 继续扩展课程内容或推进阿里云部署。

---

## 2026-08-30 新增《点集拓扑》CoursePack（competition-default）第一章语料

**更新内容**

- 在 `coursepacks/point_set_topology/competition-default/` 新增 `coursepack.json`、`knowledge.ch1.jsonl`、`problems.ch1.jsonl`；
- `coursepack.json` 声明 `coursepack_id` 为 `topology-competition-default`，`content_policy` 明确全部为团队自编内容；
- **知识节点**：第一章“拓扑空间的基本概念”共 8 个节点，覆盖拓扑空间、开集、闭集、拓扑基、由基生成拓扑、连续映射、用闭集刻画连续性、内部/闭包/边界；节点间 `relations` 目标均存在；
- **习题语料**：共 4 道全自编题（离散拓扑验证、实数通常拓扑基验证、常值映射连续性、闭包邻域交刻画），每题配置解法路径与 2 级提示阶梯；
- **版权与审查标记**：所有知识与题目 `allowed_for_training` 为 false，`review.status` 分别标记为 `needs_math_review` / `needs_ta_review`；
- **校验与导入**：JSONL 逐行解析通过，无空白行，知识与题目引用 ID 无悬空；`import-all-coursepacks` 后五门课程 CoursePack 全部入库，数据库共 41 个知识节点、29 道题目、69 条提示。

**下一步**

- 数学/TA 审校拓扑第一章 8 个节点与 4 道题目的表述正确性；
- 继续扩展数分/高代/大物/拓扑/常微分第二章或代表章节内容；
- 在 RAG/评测管线中对新增课程做召回与首提示测试。

---

## 2026-08-30 新增《常微分方程》CoursePack（competition-default）第一章语料

**更新内容**

- 在 `coursepacks/ordinary_differential_equations/competition-default/` 新增 `coursepack.json`、`knowledge.ch1.jsonl`、`problems.ch1.jsonl`；
- `coursepack.json` 中声明 `coursepack_id` 为 `ode-competition-default`，`content_policy` 明确本包全部为团队自编内容，知识节点为公共数学事实的团队转述，题目为全自编，不含教材原文与原书习题全文；
- **知识节点**：第一章“一阶常微分方程”共 7 个节点，覆盖常微分方程及其解、初值问题、通解/特解/积分曲线、可分离变量方程、一阶线性方程与积分因子、恰当方程、Picard-Lindelöf 存在唯一性定理；节点间 `relations` 目标均存在；
- **习题语料**：共 5 道全自编题目，含可分离变量初值问题、一阶线性方程通解、恰当方程求解、非利普希茨初值问题多解性分析、线性 IVP 最大存在区间判定，每题配置解法路径与 2–3 级提示阶梯；
- **版权与审查标记**：所有知识与题目 `allowed_for_training` 为 false，`review.status` 分别标记为 `needs_math_review` / `needs_ta_review`；
- **校验**：JSONL 逐行解析通过，无空白行，知识与题目引用 ID 无悬空。

**下一步**

- 数学/TA 审校第一章 7 个知识节点与 5 道题目的表述正确性；
- 将其导入 RAG/评测管线并做召回测试；
- 继续补齐剩余课程（点集拓扑）第一章 CoursePack。

---

## 2026-08-30 大学物理 CoursePack 第一章自建语料入库

**更新内容**

- 新增课程包 `physics-competition-default`（`coursepacks/university_physics/competition-default/`），覆盖 `coursepack.json`、知识节点 `knowledge.ch1.jsonl`、题目 `problems.ch1.jsonl`；
- 第一章主题为质点运动学：8 个知识节点（质点模型、位置矢量、速度矢量、加速度矢量、匀变速直线运动公式、抛体运动分解、圆周运动角量描述、伽利略速度变换），5 道全自编题（由位矢求速度与时刻、抛体射程求抛射角、圆周运动角量与切/法向加速度、船渡河相对速度、由速度求加速度与位移），每题含解法路径与 2–3 级提示阶梯；
- 版权红线执行：全部内容为团队转述/自编，不含教材原文、不含原书习题全文；`allowed_for_training` 全部关闭；知识节点标 `needs_math_review`、题目标 `needs_ta_review`，待人工核验。

**下一步**

- 将新增 CoursePack 通过 `import-coursepack` 导入并跑通首提示管线；
- 人工核验大物第一章节点与题目（物理校对 + TA 校对）；
- 继续补齐其余三门课程（高等代数、常微分方程、点集拓扑）第一章 CoursePack。

---

## 2026-08-30 新增《高等代数与解析几何》课程包（competition-default）第一章语料

**更新内容**

- **课程包创建**：在 `coursepacks/linear_algebra_analytic_geometry/competition-default/` 新增 `coursepack.json`、`knowledge.ch1.jsonl`、`problems.ch1.jsonl`；
- **内容原创声明**：`coursepack.json` 中 `content_policy` 明确本包全部为团队自编内容，知识节点为公共数学事实的团队转述，题目为全自编，不含教材原文与原书习题全文；
- **知识节点**：第一章“向量、线性组合与线性方程组”共 7 个节点，覆盖 n 维向量、线性组合、生成子空间、线性方程组、增广矩阵、初等行变换保解、解的三种情形，节点间关系完整，所有 `relations` 目标均存在；
- **习题语料**：共 5 道全自编题目，含线性组合系数求解、方程组解的类型判断、两解则无穷多解证明、生成子空间归属判断、行变换保解证明，均配置 hint_ladder 与常见错误点；
- **版权与审查标记**：所有知识与题目 `allowed_for_training` 为 false，`review.status` 分别标记为 `needs_math_review` / `needs_ta_review`；
- **校验**：JSONL 逐行解析通过，无空白行，知识与题目引用 ID 无悬空。

**下一步**

- 数学/TA 审校第一章 7 个知识节点与 5 道题目的表述正确性；
- 补齐第一章其余知识节点（行列式、矩阵运算）或进入第二章解析几何内容；
- 在 RAG/评测管线中注册该课程包并做召回测试。

---

## 2026-08-28 新增 Railway 一键部署配置（替代自购服务器）

**更新内容**

- **部署目标调整**：用户决定先不租用独立云服务器，改用 Railway 部署到 `learningmirror.cn` / `learningmirror.xyz`；
- **Railway 一体化镜像**：新增根目录 `Dockerfile.railway` + `start-railway.sh` + `railway.json`，单容器内同时运行 nginx 静态站点与 uvicorn 后端，`/api` 走同域反代；前端构建复用现有 `pnpm build:web` 静态导出；
- **pgvector 兼容**：`config.py` 增加 `database_url` 校验，自动把 Railway 提供的 `postgresql://` 改写成 `postgresql+pg8000://`；
- **部署文档**：新增 `docs/railway.md`，包含创建 pgvector 服务、设置变量、初始化数据库、绑定自定义域名与 HTTPS 的完整步骤；`README.md` 补充 Railway 为首选部署方式；
- 测试：config 校验器不破坏既有配置，全 suite 37 项仍通过。

**下一步**

- 用户在 Railway 创建项目并授权 GitHub 仓库后，按 `docs/railway.md` 完成首次部署；
- Git LFS 教材 PDF 下载完成后，在 Railway Console 执行 `import-all-textbooks` 灌入语料；
- 若 Railway 单容器资源不足，再拆分为独立 Web / API 服务。

---

## 2026-08-28 域名注册 + 生产部署配置 + 教材语料入库管道

**更新内容**

- **品牌与域名确定**：产品英文名 Learning Mirror（学镜），注册 `learningmirror.cn` 与 `learningmirror.xyz`；
- **云服务器部署配置**：新增 `compose.prod.yml` + `nginx.conf` + `apps/api/Dockerfile`，支持 Docker Compose 一体化部署到 Linux 云服务器；前端改为静态导出（`next.config.js` `output: export`），API 地址默认相对路径 `/api`，同域 nginx 反代无需写死域名；后端 CORS 改为 `MIRROR_CORS_ORIGINS` 环境变量可配置；新增 `docs/deploy.md` 完整部署指南；
- **教材语料入库**：新增 `TextbookChunk` 表与 `textbook_ingest.py` 抽取/切片模块；`retrieval.py` 新增中文 bigram 关键词检索 `search_textbook_chunks`，按命中 token 数排序；`mirror_service.py` 在统一请求管线中自动补充教材块作为上下文与引用；新增 `import-textbook` / `import-all-textbooks` CLI，已登记 7 本授权教材映射（数分上下册、高代上下册、大物上册、拓扑、常微分）；
- **授权与版权可追溯**：每个 TextbookChunk 的 `source` 字段完整记录 `allowed_for_rag/eval/training`、`retention_policy`、`license_note`；`allowed_for_training` 默认 false；
- **测试**：新增 6 项 TextbookChunk 测试，全 suite 37 项通过，ruff 干净；前端 `pnpm build:web` 静态导出验证通过。

**下一步**

- 租用云服务器并完成首次生产部署，绑定域名；
- 等 Git LFS 教材下载完成后执行 `import-all-textbooks`，抽检数分第一章抽取质量；
- 若公式抽取质量不达标，替换为 `pymupdf`/`marker` 或接入 OCR；
- 课程建设端页面继续推进。

---

## 2026-08-28 教师/TA 端最小可用版：作业工作区垂直切片闭环（AI 候选 → TA 校准 → 教师决定 → 周报）

**更新内容**

- **三端进度 2/3**：新增教师/TA 端页面 `/teacher`（首页加入口），用真实数据链路跑通产品文档的核心协同链：学生求助 → 证据落库（匿名参与码）→ 工作区聚合 → AI 产出候选班级现象 → TA 三选一校准（确认存在问题 / AI 判断有误 / 忽略）→ 教师最终决定（接受 / 忽略）→ 周报只呈现教师接受的现象；
- **学生端接入**：浏览器本地生成匿名参与码（不含姓名学号，页面可见），输入教师加入码即挂载到作业，此后求助自动携带参与码与工作区归属；附隐私提示（教师只见班级统计，不见对话内容）；
- **数据模型与管线**：新增 `assignment_workspaces` / `workspace_findings` 表，`mirror_events` 增参与码与工作区归属列；工作区校验（不存在/已关闭 404、课程不匹配或缺参与码 400）放在幂等回放检查之后——已落库 request_id 的回放无条件成功；
- **隐私红线在数据层强制**：教师/TA 端只允许聚合查询（参与码仅 distinct 计数），任何响应禁止返回学生回答与请求原文；模型输出里含参与码取值的行整行丢弃；不做个体拆分与活跃排行；周报固定附覆盖声明（“仅覆盖 N 名主动参与学生，不外推全班”）与证据通道声明（仅过程证据、作业作品通道未接入）；现象措辞假设式；生成时刻聚合快照冻结进 basis，工作区事件增长后周报仍可复现；
- **决策链状态机**：TA 在教师处理前可改判（留痕取最后一次），教师仅对 TA 已确认项做一次性最终决定，非法流转一律 409；新增 10 个 REST 端点与 `seed-demo-workspace` CLI 播种命令（8 名学生 14 条事件，请求 id 固定、幂等重跑）；
- **真实模型验证**：DeepSeek（`deepseek-v4-flash`）对演示工作区产出 3 条假设式候选现象，覆盖数正确、无参与码泄露；既有学生提示链路不受影响；
- **测试 31 项全绿**（新增 12 项：校验矩阵、关闭后回放、总览数字与隐私断言、泄露行丢弃、两级决策状态机、周报固定文案），ruff 干净；PostgreSQL schema 整体重建后重新初始化核验。

**下一步**

- 三端最后两端：课程建设端页面从骨架走向可用；
- C 组续：第一章语料人工核验、向量混合检索；
- B 组：报名截止 09-15（剩 18 天），团队名单与赛道确认宜尽快落定。

## 2026-08-28 学生端交互页面上线，智能体接入 DeepSeek 真实模型

**更新内容**

- **agent 首次在浏览器可见可演示**：新增学生端页面 `/student`（首页加入口）——左侧第一章题库（10 道自编题，标注题型与提示级数），右侧对话区支持"我卡住了给提示 → 再提示一级（阶梯用完自动置灰）→ 看完整解答思路 → 知识点答疑"；每条回答附课程引用、Harness 质检状态与学习证据，回答用 Markdown + KaTeX 渲染（标题/列表/行内与块级公式）；
- **接入真实大模型**：模型网关由占位实现切到 DeepSeek（`deepseek-v4-flash`，密钥仅存本机 `.env`，已确认不入库）；系统提示词按交互模式拆分规则（提示模式保持最小有效提示，完整解答模式给完整逐步解答），修复了"完整解答也只给第一步提示"的问题；新增 `MIRROR_LLM_TIMEOUT` 配置（默认 120 秒）适配带推理链模型；
- **新增接口**：`GET /api/v1/problems`（学生端选题，仅运行时授权题目 + 提示阶梯级数）、放行本机前端 3000 端口 CORS；
- **红线核验**：真实模型逐题跑全部 10 题的首级提示，"提示不泄露解法关键步骤"与"引用存在"质检全部通过；19 项测试全通过。

**下一步**

- 人工核验第一章节点与题目（数学校对 + TA 校对）；
- 检索升级为向量混合检索（pgvector 已就绪）；
- 教师/TA 端与课程建设端页面从骨架走向可用；正版教材到手后按授权入库。

## 2026-08-28 数分旗舰 CoursePack：第一章自编语料入库（11 知识节点 + 10 自编题）

**更新内容**

- 数分旗舰课程包从“schema 样例”升级为“第一章自建版”（编号转正为 `analysis-chen-jixiu-3e`）：数列极限主题 11 个知识节点（4 定义 + 7 定理：ε-N 定义、唯一性、有界性、四则运算、夹逼、单调有界、波尔查诺—魏尔斯特拉斯、柯西准则）+ 10 道全自编题（ε-N 直接证明、发散证明、根式差有理化、和式夹逼、递推数列单调有界、Cesàro 均值、有界乘无穷小、奇偶子列合并、乘积极限定义证明），每题含解法路径与 2–3 级提示阶梯；
- 版权红线执行：全部内容为团队转述/自编，不含教材原文、不含原书习题全文；全部条目授权字段关闭训练；知识节点标 `needs_math_review`、题目标 `needs_ta_review`，待人工核验。团队已确认有正版教材，到手后按授权范围另行登记入库并对齐节次；
- CoursePack 管道增强：manifest 支持多内容文件（按章拆分，单文件字段向后兼容）、知识节点/题目 id 包内查重、提示阶梯级别必须从 1 起严格递增，错误按文件名与行号汇报；
- 19 项测试全通过（新增语料形态、提示泄露红线、重复 id 拒绝、阶梯级别拒绝用例）；真实 PostgreSQL 重建导入核对计数（11/10/21），首提示→升级提示管线冒烟通过。

**下一步**

- 搭建学生端最小交互页面，让 agent 在浏览器可见、可演示；
- 人工核验第一章节点与题目（数学校对 + TA 校对）；
- 接入真实大模型 API（待密钥）；正版教材到手后走授权入库。

## 2026-08-27 阶段 1 基座重建完成：数据库 + 统一请求管线 + CoursePack 管道

**更新内容**

- 数据库层：10 张表（课程、课程 Profile、CoursePack、知识节点、题目、提示阶梯、题目-知识关联、镜像事件、学习证据），SQLAlchemy 模型为单一事实来源；驱动选 `pg8000`（ARM64 Windows 无 psycopg 可用轮子）；
- CoursePack 导入管道：schema 校验（按行号报错）、引用完整性检查、授权字段原样保存、幂等覆盖导入；运维 CLI（init-db / seed-profiles / import-coursepack / status）；
- 通用 Course Mirror 请求管线（`POST /api/v1/course-mirror/requests`）：课程/Profile 定位 → 题目与知识检索（`allowed_for_rag` / `allowed_for_runtime` 双门控）→ 提示级别按历史事件递增并封顶 → 模型网关生成 → Harness 检查 → 事件与证据落库；按 `request_id` 幂等；
- 模型网关：确定性占位实现（离线可测）+ 国内通用大模型 OpenAI 兼容接入位（`.env.example`）；平台级“提示泄露”与“引用存在”检查真实执行；
- 测试 15 项全通过（SQLite 开启外键约束，行为对齐 PostgreSQL）；真实 PostgreSQL 17 + pgvector 环境端到端冒烟验证通过（首提示→二级提示→幂等回放→404 路径）。

**下一步**

- 数分旗舰深链路：选定代表章节，补全知识节点/题目/解法路径/提示阶梯（C 组）；
- 模型网关接入真实大模型 API（申请密钥后改配置即可）；
- 检索升级为向量混合检索（pgvector 基础设施已就绪）。

## 2026-08-27 本机开发环境全面就绪（含 Docker 基础设施）

**更新内容**

- 在 Windows ARM64（Snapdragon X Elite）本机完成全栈环境搭建：
  - 后端 `.venv` 依赖安装完成，3 项契约测试通过，uvicorn 可启动全部阶段 0 接口；
  - 前端 `pnpm install` + 构建通过；
  - 安装 Docker Desktop ARM64 版，`docker compose up -d postgres redis minio` 成功：PostgreSQL 17（pgvector 0.8.6 已验证可建扩展）、Redis 7、MinIO 均健康运行；
- 国内网络适配（团队其他机器可复用）：
  - pip 用清华镜像 `https://pypi.tuna.tsinghua.edu.cn/simple`；
  - pnpm registry 设为 `https://registry.npmmirror.com`；
  - Docker 配置 registry-mirrors（daocloud / 1panel / rat.dev）后可正常拉取镜像；
- 依赖修正：`uvicorn[standard]` 改为 `uvicorn`（httptools 无 Windows ARM64 预编译轮子，已提交）。

**下一步**

- 启动阶段 1 底座重建：数据库 schema + 通用 Course Mirror 请求管线 + CoursePack 导入管道。

## 2026-08-27 建立项目概况总览看板与文档地图

**更新内容**

- 新增 `docs/项目概况总览.md`：一段话愿景、最终场景验收标准、比赛/产品双目标、按工作流的进度表、优先级路线、风险清单，作为全项目唯一入口看板；
- 新增 `docs/README.md` 文档地图，解决文档分散问题；`README.md`、`CLAUDE.md` 已加入口指引。

**下一步**

- 进度有大变化时先更新概况总览；
- 继续等待：代码找回方式确认（A 组）、报名与团队确认（B 组）。

## 2026-08-27 归档选题探索与实施交流记录；进度整理与下阶段任务清单（待审查）

**更新内容**

- 归档两份关键过程文档至 `docs/archive/`（原文未改动，校验一致）：ChatGPT 选题探索记录、Codex 实施交流记录，附归档说明；
- 产出进度整理 `docs/progress/进度整理-2026-08-27-现状与下阶段任务.md`：确认产品设计已定稿（v0.2 参照文档）；工程实现已完成阶段 0–3 与 v5 演示闭环、v6 真实内测系统（30 项测试 + 真实 HTTP 链路验收）；数分有 1 个审核题目可用于技术内测；
- 识别关键缺口：**阶段 1–3 与 v5/v6 代码全部在另一台机器（`C:\Users\19900\learning-mirror-platform`），从未推送入本仓库**，本机已验证该目录不可访问；
- 列出下阶段任务清单五组：A 代码找回与仓库统一、B 大赛事务（报名截止 09-15）、C 数分旗舰 CoursePack、D 内测打磨（校方审批等合规项宜早启动）、E 四门课程扩展（严格限 5 门）。

**下一步**

- 任务清单经审查确认后执行：A 组（代码找回）最优先，B 组同步启动；
- 待确认决策：代码转移方式、参赛赛道（建议"智能体"）、团队与双负责人、任务取舍与优先级。

## 2026-08-27 仓库与工作流建立

**更新内容**

- 归档大赛预通知原文与参赛指南（`docs/competition/`），梳理关键日期与行动清单；
- 建立项目工作流（`WORKFLOW.md`）：提交规范、重大更新定义、tag 快照策略；
- 建立本进度日志与 `CLAUDE.md` 协作规则；
- 确认仓库基线：阶段 0 骨架已就位（五门课程注册表、Course Mirror 协议、三端页面骨架、本地基础设施配置，见 `README.md`）。

**下一步**

- 确认选题表述与参赛赛道；
- 按 `docs/development-roadmap.md` 启动阶段 1（五门课程共同底座 + 数分首条深链路）；
- 官网报名与下载中心材料归档（截止 09-15）。
