# 2026-09-20 AI 学习首版（本地）

## 基线

- 本轮开始时本地 main 为 27e5e93；从 origin/main 安全快进至 4f69015，保留队友注册、HTTPS、本地课表连接器等更新。
- 未改动用户新增的 materials/教材/人工智能基础.pdf，也不提交 PDF、教材节选、本地数据库或密钥。
- 本轮代码目录：C:/Users/19900/Projects/mathmirror。
- 用户已同意正式发布与切换服务器模型。服务器连通，但 ubuntu 登录拒绝现有本机 SSH 密钥；已询问有效的本地私钥路径/登录方式。正式网站尚未部署，不绕过认证。

## 已实现并验证

- 本地 .env 的文字模型切换为 deepseek-flash；视觉默认模型与生产 compose 的可配置默认值也切换为 deepseek-flash。
- DeepSeek 官方视觉文档：https://api-docs.deepseek.com/guides/vision/ 。真实封面识图、AI 图片聊天、教材检索问答和原数分问答均成功。密钥仍只在服务端。
- 学生端新增 /student/ai、/resources、/notes、/tools、/setup 五个独立路由（均在 /student/ai 下），沿用当前 Portal UI。
- 独立 AI 会话与消息表，直接回答、连续对话、图片上传和手机拍照文件输入；不创建作业空间，不写 Attempt、Observation 或 LearningEvidence。旧 v1 AI 提示入口返回 410。
- 图片仅随本轮模型请求发送，不保存原图；历史显示附图标记，后续需要重新读图时须重传。该边界不改变正式作业原图保存的待办。
- 私人笔记支持回答转存前编辑确认、自建、修改、删除、Markdown 导出；账号总导出、账号删除与保留期清理包含 AI 数据。教师不能查看他人 AI 对话或笔记。
- 同一 API worker 内限制每账号一个生成请求，重复 request_id 回放已保存结果；多 worker 分布式锁/限流尚未实现。
- 教材候选与 reviewed 内容隔离，仅后者可进入检索；当前为词项/中文二元词召回，不是成熟的向量混合检索。
- 工具中心覆盖对话、生图、视频、音乐、文案、办公、编程的官网入口；没有接入这些工具的站内生成 API。
- Codex、Claude Code、WorkBuddy、CC Switch 提供官方/原作者安装文档与安全指引；没有实现本地一键安装程序，也没有在用户电脑上安装这些工具。

## 教材实际状态

- 邵军力、张景、魏长华《人工智能基础》，电子工业出版社，2000 年 3 月第 1 版。
- 358 个 PDF 页面，为扫描图像，无嵌入文本。旧教材适用于经典知识，不应作为最新工具规格依据。
- 已登记文件 SHA-256 和总页数；PDF 第 15 页（原书第 1 页）“什么是人工智能”节选已逐字目视核对并入本地数据库。
- 用户随后确认只录入 PDF 第 1—264 页。已将这个范围实际写入本地 AI 教材库：263 页候选 + 1 处目视核对节选；仅后者进入可靠问答检索，绝非全书通过审校。例题、习题仍为离线候选，试卷尚未导入。
- 新脚本 scripts/import-ai-textbook.py 可登记 PDF，按版本哈希导入候选；只有显式 --reviewed-by 才发布已核对内容。该参数应仅用于已经完成原图校验的材料。
- 用户已批准单独 20 元教材 API 上限。独立账本 data/ai-textbook/budget-20260920.sqlite，按官方高峰输入 2 元/百万、输出 8 元/百万保守计价，不享受缓存和空闲时段折扣；原数分 100 元账本未动。
- 已完成 358 页首轮扫描、5 页补识别及 358 页独立机器原图复核，共 721 次账本内调用，保守累计 8.67638 元，未知费用 0。复核为 match 136、issues 216、unreadable 1、格式/截断异常 5；match 不是教师审校，更不是教学正确性认证。
- 源文件完整性检查发现：PDF 第 264—358 页是同一张原书第 250 页，只有 264 个不同的整页图像。第 265—358 页共 94 页为重复，后半册缺失。已用 PDFium 整页像素哈希及 pypdf 内容流交叉验证，并目视确认。
- 已停止进一步付费。用户选择暂不处理后半册，仅按 PDF 第 1—264 页建设，取消“必须等待完整替换件”的阻塞。扫描器仍阻止对重复源全量重扫；新增 check-ai-textbook-integrity.py，后续任何新教材必须先本地检查再付费。
- 原题整理器排除目录和重复页面，仅对明确相邻且有续页标记的片段归并，生成候选章节、题号、原书页码/PDF页码和缺页缺号清单。产物仅在 data/ai-textbook 下，不提交原文，也未将整本候选发布进问答。
- 当前索引覆盖 12 个已出现的章节标题、139 个原题候选（46 例题、93 习题）；16 个候选有缺号/续页等标记，不等于 139 道完整准确且已发布的题目。
- 老数分扫描/审计脚本的历史视觉模型和计价账本未迁移，本轮没有运行；再启用前必须核对当前官方价格、图像 token 上限和预算预留，不能沿用旧 384 token/图假设。

## 验收

- 后端完整 pytest：111 passed（隔离临时 SQLite，测试不使用真实密钥）。
- 前端 Next.js：25 个静态页面构建成功，新增四个 AI 子路由。
- Edge/Playwright：真实账号、教材出处第 15 页、两轮文本/图片模型调用、笔记创建/编辑/导出成功；五个路由在 390px 宽度无横向溢出，零 pageerror。
- 桌面聊天与手机页面已目视检查。本轮不是手机真机摄像头测试，不代表 HEIC 支持或所有手机兼容性验收。
- 浏览器脚本首次清理缺少 confirmed 字段，已修复；原合成账号已定向清理。另建无模型调用的测试账号，确认删除返回 200，删除后访问返回 401。
- 本地后端 127.0.0.1:8010，前端 127.0.0.1:3010。启动命令从仓库根读取 .env；从 apps/web 启动 Python 不会读取根配置。

## 后续

1. 按用户确认范围继续校正前264页的问题，完善题号与章节映射，审校后分批发布；后半册暂不处理。候选入库不等于准确的 RAG 覆盖。
2. 扩展现代 AI/LLM 官方资料来源，分清历史教材与当前实践；加入检索质量、引用正确性、无依据拒答评测。
3. 例题习题与试卷的数据模型、审校流程、检索页面；教材全文展示须另核对传播授权。
4. 工具集成的供应商 API、学生额度、计费与数据边界；不能把官网导航视为一站式调用已经完成。
5. 本地部署助手：环境检测、签名安装包/明确同意、配置备份、兼容性测试、失败恢复与卸载。不通过网页任意执行远程脚本。
6. 发布已获确认，等待有效 SSH 登录方式；连通后先备份服务器数据库和配置，核对服务器工作树，更新模型环境变量，部署、烟测与回退准备。不执行未经检查的 git reset --hard。

## 官方安装来源（2026-09-20 核对）

- https://developers.openai.com/zh-Hans/docs/codex/cli
- https://code.claude.com/docs/en/setup
- https://www.codebuddy.cn/docs/workbuddy/From-Beginner-to-Expert-Guide/Installation-Win-Guide
- https://github.com/farion1231/cc-switch
